# Historical results

These small files preserve the metrics referenced by the audit. They are exploratory historical outputs, not reproduced EventLens benchmark results. They do not prove forecasting uplift and may refer to different targets, datasets and validation methods. The audit describes their limitations.

Plots, model checkpoints, raw responses and large generated datasets remain local. Future experiments should save outputs to a separate ignored `runs/<run_id>/` directory.
